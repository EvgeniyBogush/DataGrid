namespace Eneca.CustomDataGrid.Models
{
    /// <summary>Visual interaction state of a header cell when <see cref="Controls.EnecaDataGrid.EnableHeaderSelection"/> is enabled.</summary>
    public enum DataGridHeaderVisualState
    {
        Default,
        Hover,
        Pressed,
        Disabled,
        Active,
        Clicked
    }
}
